/**
 * Parent-page iframe size when the booking chatbot is open.
 * Keep roughly aligned with FloatingBookingChatbot open panel height + margins.
 */
export const CHATBOT_EMBED_IFRAME_OPEN = {
  width: "400px",
  height: "min(820px, 100dvh)",
} as const;
