services:
  app:
    # `image:` lets production `docker compose pull && up -d` grab the
    # CI-built image from ghcr.io instead of running `next build` on this box.
    # `build:` is kept for local/dev `docker compose up --build`.
    image: ghcr.io/reviewrise/ai-assistant-app:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: ai_assistant_app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env.production
    command: sh -c "sh scripts/db-sync.sh && node server.js"
    ports:
      - "3015:3000"
    volumes:
      - ./data/uploads:/app/public/uploads
    networks:
      - default
    healthcheck:
      test: ["CMD-SHELL", "curl -f -H \"X-Health-Token: $${HEALTH_CHECK_TOKEN}\" http://localhost:3000/api/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 120s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 384M

  retell-llm:
    image: ghcr.io/reviewrise/ai-assistant-app:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: ai_assistant_retell_llm
    restart: unless-stopped
    profiles:
      - retell-custom-llm
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env.production
    command: node dist/retell-custom-llm-server.js
    ports:
      - "3017:3001"
    networks:
      - default
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  postgres:
    image: postgres:16-alpine
    container_name: ai_assistant_postgres
    restart: unless-stopped
    env_file:
      - .env.production
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 384M

volumes:
  postgres_data:

