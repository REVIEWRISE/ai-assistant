# dependencies
node_modules

# env — ignore everything, then explicitly allow committed templates
.env*
!.env.example
!.env.production.example

# next.js
.next/
out/

# misc
.turbo
*.tsbuildinfo

# Production upload volume (bind-mounted in docker-compose.prod.yml)
data/uploads/
